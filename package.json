{
  "name": "online-store-backend",
  "version": "1.0.0",
  "description": "Backend API server for Custom Threads Online Store",
  "scripts": {
    "build": "echo === STARTING ROOT BUILD === && cd server && npm install && npm run build && echo === ROOT BUILD COMPLETE === && ls -la server/dist/server.js",
    "postinstall": "npm run build",
    "start": "cd server && node dist/server.js"
  },
  "author": "",
  "license": "MIT",
  "keywords": []
}
