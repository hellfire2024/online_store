// This file contains simplified route stubs. Each can be expanded with full CRUD operations.

import { Router } from 'express';
import { createSimpleRoutes } from '../utils/routeHelpers.js';

export default createSimpleRoutes('customers');
